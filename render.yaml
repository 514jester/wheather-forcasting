services:
  - type: web
    name: weather-forecasting
    runtime: python
    buildCommand: pip install -r requirements.txt && python generate_dataset.py && python train_models.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
